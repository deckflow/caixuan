{
  "name": "caixuan-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --sort build",
    "dev": "pnpm --filter caixuan dev",
    "test": "pnpm -r --sort test",
    "test:unit": "pnpm -r --sort test:unit",
    "typecheck": "pnpm -r --sort typecheck",
    "install:global": "node scripts/install-global.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prettier": "^3.1.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
